
VITE_Api_URL=http://localhost:3000/solar-api
# Optional: set to 'true' during development to bypass login and auto-auth as a demo Admin user
# Disable by default so reloads require real authentication
VITE_DEV_BYPASS=false